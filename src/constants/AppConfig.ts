/**
 * Application-wide configuration settings
 */

const AppConfig = {
  // Authentication settings
  auth: {
    // Set to true to persist authentication data in AsyncStorage
    persistAuth: true,
  },
};

export default AppConfig;
